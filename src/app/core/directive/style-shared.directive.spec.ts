import { StyleSharedDirective } from './style-shared.directive';

describe('StyleSharedDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleSharedDirective();
    expect(directive).toBeTruthy();
  });
});
